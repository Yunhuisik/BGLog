spring.application.name=bglog
server.port=${PORT:8080}

pubg.api-key=${PUBG_API_KEY}
pubg.base-url=https://api.pubg.com

spring.jackson.serialization.indent-output=true
spring.jackson.deserialization.fail-on-unknown-properties=false


# 실제 DB 쓸 때 빼기
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration 


bglog.match.parallelism=${BGLOG_MATCH_PARALLELISM:8}
bglog.match.max-detail-count=${BGLOG_MATCH_MAX_DETAIL_COUNT:200}
bglog.match.distinct-enabled=${BGLOG_MATCH_DISTINCT_ENABLED:true}

# 에러 잡고나면 빼기
# server.error.include-message=always
# server.error.include-binding-errors=always
# server.error.include-stacktrace=always

# logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.web.servlet.DispatcherServlet=DEBUG